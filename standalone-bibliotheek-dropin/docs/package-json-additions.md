Add these dependencies to the target Laravel app if they are missing:

```json
{
  "devDependencies": {
    "laravel-vite-plugin": "^2.0.0",
    "vite": "^7.0.0"
  },
  "dependencies": {
    "axios": "^1.8.0"
  }
}
```

